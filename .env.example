# Copy to .env . Never commit .env — it is gitignored.
OPENROUTER_API_KEY=sk-or-v1-...
ARTIFICIALANALYSIS_API_KEY=aa-...

# Hard cap on Artificial Analysis requests per calendar day.
# Their free tier is 100/day; 3 is plenty because responses are cached to disk.
AA_DAILY_BUDGET=3
